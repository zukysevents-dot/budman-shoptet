node_modules/

# Tajné přístupy – NIKDY necommitovat
migration/config/.env
.env

# Pracovní data migrace (raw export, normalizovaná data, výstupy importu)
# – generují se skripty, do gitu nepatří. Fixtures jsou verzované zvlášť.
migration/data/raw/
migration/data/normalized/
migration/data/out/

# Build šablony
assets/dist/
dist/

# Playwright / preview artefakty
.playwright-mcp/
*-preview.png

# Full-res logo (4,9 MB) – odvozené z PDF, regenerovatelné. Web verze (budman-logo-web.png) se verzuje.
assets/brand/budman-logo.png

# OS / editor
.DS_Store
Thumbs.db
