# src/js (Fáze 2)

Vanilla JS pro custom šablonu (scroll-reveal přes IntersectionObserver, sticky header, hover karet).
Žádný React/Vue, žádné `document.write`, UTF-8. Build → `assets/budman.js` → Shoptet FTP.
Port z `Budman/src/js/modules/` (reveal, age-gate koncept) — age-gate řeší doplněk 18+, ne vlastní kód.
